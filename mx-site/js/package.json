{
  "type": "module",
  "_comment": "Tells Node these files are ES modules and silences MODULE_TYPELESS_PACKAGE_JSON when tests/test-pdf-inspector.js loads pdf-inspector-core.js. The browser ignores package.json for static file serving, so this is Node-only metadata. All sibling .js files here are written as ES module-safe code (either explicit import/export or self-contained IIFEs)."
}

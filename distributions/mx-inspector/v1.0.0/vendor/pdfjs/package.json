{
  "type": "module",
  "_comment": "Tells Node these vendored pdfjs files are ES modules and skips the MODULE_TYPELESS_PACKAGE_JSON reparse warning when tests/test-pdf-inspector.js loads them. The browser ignores package.json for static file serving, so this is Node-only metadata."
}

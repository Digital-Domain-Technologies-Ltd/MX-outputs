# Manuscript uniqueness — operator ignore list
#
# Paragraph hashes listed here are excluded from ALL duplicate reporting
# (cross-book and within-book). Use it for paragraphs that are intentionally
# shared or repeated — boilerplate, legal notices, defined terms — and that
# you do not want flagged.
#
# Format: one SHA-256 paragraph hash per line. Blank lines and lines starting
# with '#' are ignored. The hash for any flagged paragraph is shown in the
# report (the `code` after the word count) and in json/manuscript-index.json.
# Hashes are computed on a canonical form (lower-cased, punctuation and
# whitespace removed), so one entry covers every case/punctuation variant.
#
# Example (remove the leading '#' to activate):
# eaf9b4a1f667...full64charhash...   # llms.txt structural-problem boilerplate
#
# No active entries yet.

{
  "patches": [
    {
      "find": "**Finding:** Security headers absent: HSTS, CSP, X-Frame-Options, X-Content-Type-Options (Site-wide). Missing security h",
      "findingIndex": 1,
      "justification": "scope: replaced site‑wide with across the audited set to correct scope violation.",
      "replace": "**Finding:** Security headers absent: HSTS, CSP, X-Frame-Options, X-Content-Type-Options (across the audited set). Missing security h"
    },
    {
      "find": "- Add the missing response headers at the server or CDN edge; each is a one-line directive that applies site-wide once c",
      "findingIndex": 2,
      "justification": "scope: replaced site‑wide with across the audited set in recommendation.",
      "replace": "- Add the missing response headers at the server or CDN edge; each is a one-line directive that applies across the audited set once c"
    },
    {
      "find": "**Finding:** Heading levels skip on 1 audited page(s) (for example an h2 followed by an h4), so the document outline a machine or screen reader builds does not match the visible structure.",
      "findingIndex": 3,
      "justification": "absence‑as‑positive: named page path inline for single-page finding.",
      "replace": "**Finding:** Heading levels skip on `/` (for example an h2 followed by an h4), so the document outline a machine or screen reader builds does not match the visible structure."
    },
    {
      "find": "On the audited page at https://atmors.netlify.app, we found SEO scoring 84/100, indicating robust optimisation that helps machines discover",
      "findingIndex": 4,
      "justification": "marketing‑inflation: clarified benefit without inflated language.",
      "replace": "On the audited page at https://atmors.netlify.app, we found SEO scoring 84/100, which indicates that search engines can index and surface the page effectively."
    }
  ],
  "unfixable": [
    5
  ]
}
{
  "patches": [
    {
      "find": "**Finding:** Security headers absent: CSP, X-Frame-Options, X-Content-Type-Options (Site-wide). Missing security headers.",
      "findingIndex": 1,
      "justification": "voice: rewrote 'Site-wide' to 'across the audited set'",
      "replace": "**Finding:** Security headers absent: CSP, X-Frame-Options, X-Content-Type-Options (across the audited set). Missing security headers."
    },
    {
      "find": "- Add the missing response headers at the server or CDN edge; each is a one-line directive that applies site‑wide once cached.",
      "findingIndex": 2,
      "justification": "scope: replaced 'site-wide' with scoped phrase",
      "replace": "- Add the missing response headers at the server or CDN edge; each is a one-line directive that applies across the audited set once cached."
    },
    {
      "find": "- **sameAs**: linking the Organisation entity to external social profiles (e.g., LinkedIn, Twitter) gives machines a verified context.",
      "findingIndex": 3,
      "justification": "scope: added scoped phrase 'across the audited set'",
      "replace": "- **sameAs**: linking the Organisation entity to external social profiles (e.g., LinkedIn, Twitter) gives machines a verified context across the audited set."
    },
    {
      "find": "Accessibility remains a Priority 1 compliance area; across the audited set we identified 11 distinct WCAG AA issue types affecting 279 eleme",
      "findingIndex": 4,
      "justification": "marketing: simplified statement to remove inflated phrasing",
      "replace": "Accessibility remains a Priority 1 compliance area; we identified 11 distinct WCAG AA issue types affecting 279 elements."
    },
    {
      "find": "- Add the missing response headers at the server or CDN edge; each is a one-line directive that applies across the audited set once cached.",
      "findingIndex": 5,
      "justification": "hollow: added specific data-backed detail about header absence",
      "replace": "- Add the missing response headers at the server or CDN edge; each is a one‑line directive that applies across the audited set once cached. The CSP, X-Frame-Options, and X-Content-Type-Options headers are absent on all 12 audited pages."
    },
    {
      "find": "We fetch every page twice: as a server-side agent sees it (raw served HTML, no JavaScript) and after full browser rendering. The gap between those two results is the served-versus-rendered gap: the share of content invisible to agents that do not execute JavaScript. Server‑side agents, including those behind ChatGPT, Claude, and Perplexity, parse served HTML only.",
      "findingIndex": 6,
      "justification": "tone: rewrote paragraph to maintain first-person voice",
      "replace": "We fetch every page twice—once as a server-side agent sees it (raw served HTML, no JavaScript) and again after full browser rendering. We then compare the two results; the difference is the served‑versus‑rendered gap, which represents content invisible to agents that do not execute JavaScript. Server‑side agents, including those behind ChatGPT, Claude, and Perplexity, parse only the served HTML."
    }
  ],
  "unfixable": []
}